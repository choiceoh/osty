// Matrix §2.1.A retest: List.find(closure). PR #1983/#1987 unblock.

use std.testing

fn testListFindClosure() {
    let xs = [4, 1, 3, 2]
    let found = xs.find(|x| x > 2) ?? -1
    testing.assertEq(found, 4)
}
