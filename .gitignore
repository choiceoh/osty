# Osty build/test output
.bin/
.osty/
**/.osty/
.profiles/
.direnv/
tmp/

# VS Code extension dependencies and packages
node_modules/
*.vsix

# Local editor and OS files
.DS_Store
.idea/
*.swp

# Per-worktree scratch directory used by IDE/agent tooling.
.claude/worktrees/
.opencode/

# opencode runtime state (node_modules, lockfiles, session state) — machine-local.
.opencode/

# graphify knowledge graph (local dev artifact, rebuild with /graphify)
graphify-out/


# osty-vs-go run history — machine-local, regenerated by running the tool.
.osty-vs-go-history.jsonl

# `go build ./cmd/osty-vs-go` artifact when built outside .bin/.
/osty-vs-go

# `go build ./cmd/osty-native-checker` artifact when built outside .bin/.
/osty-native-checker

# osty.lock for cmd/*/ — these are dep-driven manifests; cmd subprojects
# with [dependencies] empty regenerate a stale lock on each build.
# examples/<name>/main.osty.lock is a different naming and stays tracked.
cmd/*/osty.lock
