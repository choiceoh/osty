[package]
name = "toolchain"
version = "0.1.0"
edition = "0.3"
description = "Self-hosting core algorithms ported to Osty."
license = "MIT"
keywords = ["toolchain", "semver", "registry", "manifest", "diagnostics", "ir", "lint", "resolve", "check", "docgen", "llvm"]

[dependencies]
