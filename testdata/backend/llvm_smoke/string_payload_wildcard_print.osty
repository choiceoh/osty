enum Label {
    Text(String)
    Empty
}

fn main() {
    let value: Label = Text("payload string")
    println(match value {
        Text(_) -> "payload string",
        Empty -> "missing",
    })
}
